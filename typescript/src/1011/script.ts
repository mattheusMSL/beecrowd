import * as readline from "readline";
import { stdin as input} from "process"

const readLine: readline.Interface = createInterface({ input });

readLine.question("", () => {
   


 readLine.close();
})

